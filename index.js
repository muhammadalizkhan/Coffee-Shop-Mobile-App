/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Navigation from './src/RootNavigation/Navigation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Navigation);
